$('#comments').append("<%= escape_javascript(render(@comment)) %>");
$('#rate-point').html("<%= @movie.rating.round(1) %>");
$('#cmt-count').html("Có " + "<%= @movie.comments.length %>" + " đánh giá");
$('#new_comment')[0].reset();
